import { NewMessagesPipe } from './new-messages.pipe';

describe('NewMessagesPipe', () => {
  it('create an instance', () => {
    const pipe = new NewMessagesPipe();
    expect(pipe).toBeTruthy();
  });
});
